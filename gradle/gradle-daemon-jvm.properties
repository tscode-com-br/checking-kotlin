#This file is generated by updateDaemonJvm
#toolchainVendor intentionally unset: pinning to MICROSOFT broke Linux CI (no Microsoft JDK 17
#available/downloadable on the runner). Version-only criteria is satisfied by any installed JDK 17
#(e.g. Temurin on CI via setup-java, or your local Microsoft build). Do NOT re-run updateDaemonJvm
#on a Microsoft-JDK machine without removing the vendor line again.
toolchainVersion=17
