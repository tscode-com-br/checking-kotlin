package br.com.tscode.checking.presentation.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import br.com.tscode.checking.R

// Arimo (Google Fonts, OFL) — metrically compatible with Arial; clean and neutral.
// Bundled in res/font/ as a variable font; the default instance is Regular (wght 400).
// Used for the developer credit on the splash screen.
val ArimoFamily = FontFamily(Font(R.font.arimo))
